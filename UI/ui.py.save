import tkinter as tk
from tkinter import ttk as ttk

def go_home():
    print "Heading home"

def text_box_update(event):
    print event

room_list = ["3034","3032","3030"]

window = tk.Tk()
window.geometry("800x800")

# Home button frame
frameA = tk.Frame()
# Room list frame
frameB = tk.Frame()
# Robot status frame
frameC = tk.Frame()

# Message widget
greeting = tk.Label(text="Welcome to group 1 robot interface",master=frameA)
greeting.pack()

# Button widget
button = tk.Button(
    text="Go Home",
    width=25,
    height=5,
    bg="blue",
    fg="white",
    command = go_home,
    master=frameB
)

button.pack()

# Room list widget
list = ttk.Combobox(frameC, values=room_list,textvaiable=value)
list.bind("<<ComboboxSelected>>",text_box_update)
list.set("Select destination")
list.pack()

# Packing frames, order matters
frameA.pack()
frameB.pack()
frameC.pack()

# Display
window.mainloop()
